<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kronos.gestoractividades" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Principal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Secundaria"
            android:label="@string/title_activity_secundaria" >
        </activity>
        <activity
            android:name=".DetalleActividad"
            android:label="@string/title_activity_detalle_actividad" >
        </activity>
        <activity
            android:name=".IMActividad"
            android:label="@string/title_activity_imactividad" >
        </activity>
    </application>

</manifest>
